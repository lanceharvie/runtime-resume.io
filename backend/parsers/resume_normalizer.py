"""Normalize parser outputs into a shared resume schema."""


def normalize_resume(parsed_resume: dict) -> dict:
    """Convert raw parser output into a normalized resume structure."""
    raise NotImplementedError("Resume normalization not implemented yet")
