# Pipeline Worker

This worker runs the external APScheduler process for candidate pipeline jobs.

It should import shared job functions from `services/pipeline-api/app/jobs/` so API and worker behavior stay aligned.

